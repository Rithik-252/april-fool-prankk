<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>🎁 Surprise Rewards</title>
<style>
    body {
        margin: 0;
        font-family: 'Segoe UI', sans-serif;
        background: linear-gradient(135deg, #6a11cb, #2575fc);
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
    }

    .container {
        background: rgba(255,255,255,0.1);
        padding: 30px;
        border-radius: 15px;
        text-align: center;
        backdrop-filter: blur(10px);
        box-shadow: 0 10px 25px rgba(0,0,0,0.3);
        width: 300px;
    }

    input {
        padding: 10px;
        width: 80%;
        border: none;
        border-radius: 8px;
        margin: 10px 0;
    }

    button {
        padding: 10px 20px;
        border: none;
        border-radius: 8px;
        background: #ffcc00;
        cursor: pointer;
        font-weight: bold;
        transition: 0.3s;
    }

    button:hover {
        background: #ff9900;
        transform: scale(1.05);
    }

    .prizes {
        display: none;
        margin-top: 15px;
    }

    .prize {
        background: white;
        color: black;
        padding: 8px;
        margin: 5px;
        border-radius: 8px;
        cursor: pointer;
        transition: 0.3s;
    }

    .prize:hover {
        background: #ffcc00;
    }
</style>
</head>

<body>

<div class="container">
    <h2>🎁 Claim Your Reward</h2>
    <input type="text" id="name" placeholder="Enter your name">
    <br>
    <button onclick="showPrizes()">Continue</button>

    <div class="prizes" id="prizes">
        <p>Choose your reward 👇</p>
        <div class="prize" onclick="selectPrize()">💰 ₹5000 Cash</div>
        <div class="prize" onclick="selectPrize()">📱 iPhone 15</div>
        <div class="prize" onclick="selectPrize()">🎧 AirPods</div>
    </div>
</div>

<audio id="clickSound" src="https://www.soundjay.com/buttons/button-09.mp3"></audio>

<script>
function showPrizes() {
    let name = document.getElementById("name").value;
    if(name === "") {
        alert("Enter your name first!");
        return;
    }
    document.getElementById("prizes").style.display = "block";
}

function selectPrize() {
    document.getElementById("clickSound").play();

    setTimeout(() => {
        window.open("final.html", "_blank");
    }, 800);
}
</script>

</body>
</html>